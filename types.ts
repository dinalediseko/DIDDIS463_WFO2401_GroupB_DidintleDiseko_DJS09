// Define the possible prices for the product
export type Price = 25 | 30 | 35 | 45

// Define the countries where the product is available
export type Country = 'Colombia' | 'Poland' | 'United Kingdom' | 'Malaysia'
